<UserControl x:Class="OakMusic.Views.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="24,8,24,0">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <TextBlock x:Name="PlaylistTitleText"
                       Text="PLAYLISTS"
                       Foreground="#F0F0F0"
                       FontSize="18"
                       FontWeight="SemiBold"/>

            <TextBlock x:Name="PlaylistSongCountText"
                       Text=""
                       Foreground="#707070"
                       FontSize="12"
                       Margin="0,26,0,0"/>

        </Grid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,14,0,10">

            <Button x:Name="PlayAllButton"
                    Content="▶  Play All"
                    Width="100"
                    Height="36"
                    Background="#202020"
                    Foreground="White"
                    BorderThickness="0"
                    Click="PlayAllButton_Click"/>

            <Button x:Name="BackButton"
                    Content="←  Back"
                    Width="80"
                    Height="36"
                    Margin="8,0,0,0"
                    Background="Transparent"
                    Foreground="#AAAAAA"
                    BorderThickness="0"
                    Click="BackButton_Click"/>

        </StackPanel>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

            <StackPanel x:Name="PlaylistSongsPanel"/>

        </ScrollViewer>

    </Grid>

</UserControl>